# Core dependencies for MARCUS-Avatar inference
# Pins below match the verified runtime (kept in sync with pixi.toml).

# Deep learning / diffusion
torch>=2.0.0
torchvision
torchaudio
# LongCat-Image official environment. diffusers>=0.36 ships a native
# LongCatImageTransformer2DModel which shadows the vendored class and breaks loading.
diffusers==0.35.2
transformers==4.57.1
accelerate==1.11.0
peft==0.18.0
huggingface_hub>=0.34.0,<1.0
safetensors
einops
sentencepiece

# Face / vision
opencv-python
mediapipe
tensorflow
mtcnn
albumentations
kornia
timm
scipy
scikit-image
Pillow
imageio
imageio-ffmpeg
realesrgan>=0.3.0

# 3D / rendering
numpy
trimesh
pygltflib

# Gradio demo
gradio>=4.0

# Utilities
tqdm
easydict
torchsummary

# ---------------------------------------------------------------------------
# Post-install note (Real-ESRGAN / BasicSR):
# basicsr 1.4.2 does `from torchvision.transforms.functional_tensor import ...`,
# a module removed in torchvision>=0.18. Patch the installed file once:
#
#   sed -i 's|torchvision\.transforms\.functional_tensor|torchvision.transforms.functional|' \
#     "$(python -c 'import basicsr, os; print(os.path.dirname(basicsr.__file__))')/data/degradations.py"
#
# (pixi users: `pixi run app` / `pixi run batch-infer` apply this automatically
# via the `patch-basicsr` task.)
# ---------------------------------------------------------------------------
